#+startup:fold -*- buffer-read-only:t -*- 
* ------------------------------------------------------------------------------
* installation under spack                                                   
- <2026-04-10 Fri> add two mode packages:
  - node-js
  - npm              # package manager
  - spdlog           # supposedly, smart handling of log files - not using yet
- last update: <2025-11-24 Thu> - versions are time-dependent
  fermi 0.23: remotes/origin/v0.23.0-fermi
  spack 0.23: tags/v0.23.0
- do not use spack v1.0, use Eric's 0.28 version
- the installation script takes two parameters:
  the created subrirectory name and the name of the created spack environment
#+begin_src
  curl -o install_daq.sh  https://raw.githubusercontent.com/pavel1murat/frontends/refs/heads/main/scripts/install_daq.sh
  source install_daq.sh <dir_name> <env_name>
#+end_src

[[file:../scripts/install_daq.sh]]
  
#+begin_src
mkdir daquser_003
cd daquser_003
unset SPACK_ROOT
git clone https://github.com/mu2e/spack.git
pushd spack; git checkout eflumerf/FixPerlPackageStash ; popd
export SPACK_DISABLE_LOCAL_CONFIG=true
source spack/share/spack/setup-env.sh
env_name=v001                                # replace v001 with your own name, i.e. 'v001'
spack env create   $env_name
spack env activate $env_name
ln -s spack/var/spack/environments/$env_name
mkdir $env_name/build
#------------------------------------------------------------------------------
# build in $env_build, not in /tmp
#------------------------------------------------------------------------------
export         TMP=$SPACK_ENV/build
export      TMPDIR=$SPACK_ENV/build
#------------------------------------------------------------------------------
# repositories
#------------------------------------------------------------------------------
mkdir spack-repos
pushd spack-repos
git clone https://github.com/FNALssi/fnal_art.git
cd fnal_art; git checkout 8df014c32544bc72f9d27e39b0d190a5a2a03e91 ; cd ..;
git clone https://github.com/marcmengel/scd_recipes.git
cd scd_recipes; git checkout c0328997e3f48b0b1341f8ea7ebacea95f9b7472 ; cd ..;
git clone https://github.com/art-daq/artdaq-spack.git
git clone https://github.com/Mu2e/mu2e-spack.git
git clone https://github.com/pavel1murat/spack_packages murat-spack
for d in `ls` ; do echo $d; spack repo add $d ; done
popd
#+end_src
# ------------------------------------------------------------------------------
# comment out everything *-suite related in mu2e and artdaq recipes
# packages involved:
#
# to handle (a spack/Eric of a spack 0.23 error?) declare gcc @11.5.0 as a package
# in ~/.spack/bootstrap/config/packages.yaml
# otherwise, it doesn't concretize
#
# add CPU target (today - x86_64_v3) to the $spack_env/spack.yaml, also use system-wide krb5, openssl, and opengl
# keep using gcc 13.1.0 , problems otherwise.
# concretize --deprecated is a key
#
# a this point copy spack/etc/spack/upstreams.yaml from where it is....
# or may write a script making the upstreams file starting from /mu2e/spack_areas
# ------------------------------------------------------------------------------
cp ~mu2eshift/ots_ops/spack/etc/spack/upstreams.yaml $SPACK_ROOT/etc/spack/.
# ------------------------------------------------------------------------------
# after that ;
# a) rust needs /usr/lib64/pkgconfig to be included, and for some reason it is not included
# ------------------------------------------------------------------------------
# c) create env variable pointing to the view (MIDAS needs that)
#    midas+sqlite fails on the first pass, suceeds on the second, w/o any changes ... why ???
#    just do it twice....
# ------------------------------------------------------------------------------
export      SPACK_VIEW=$PWD/$env_name/.spack-env/view
export PKG_CONFIG_PATH=$PKG_CONFIG_PATH:/usr/lib64/pkgconfig
#------------------------------------------------------------------------------
# a) copy spack.yaml to where it belongs, check versioins of the packages used
# b) patch recipes: gdk-pixbuf, fhicl-cpp, and geant4
# gdk-pixbuf recipe needs to be patched before the emacs installation
# however in the very beginning the repos/builtin doesn't exist
# can this be avoided by configuring the packages ?
# ------------------------------------------------------------------------------
spack add     frontends@main
spack develop frontends@main
cp $env_name/frontends/scripts/spack.yaml $env_name/.
# ------------------------------------------------------------------------------
# b) patch recipes: gdk-pixbuf, fhicl-cpp, and geant4
# gdk-pixbuf recipe needs to be patched before the emacs installation
# however in the very beginning repos/builtin doesn't exist
# can this be avoided by configuring the packages ?
# ------------------------------------------------------------------------------
#+begin_src
patch -i $env_name/frontends/scripts/gdk-pixbuf-recipe.patch -o spack/var/spack/repos/builtin/packages/gdk-pixbuf/package.py
patch -i $env_name/frontends/scripts/fhicl-cpp-recipe.patch  -o spack-repos/fnal_art/packages/fhicl-cpp/package.py
patch -i $env_name/frontends/scripts/geant4-recipe.patch     -o spack/var/spack/repos/builtin/packages/geant4/package.py
#+end_src
# ------------------------------------------------------------------------------
# a this point copy spack/etc/spack/upstreams.yaml from where it is....
# or may write a script making the upstreams file starting from /mu2e/spack_areas
# ------------------------------------------------------------------------------
cp ~mu2eshift/ots_ops/spack/etc/spack/upstreams.yaml $SPACK_ROOT/etc/spack/.

# spack add     node-js
spack concretize -f                    # success
# ------------------------------------------------------------------------------
# b) patch recipes: gdk-pixbuf, fhicl-cpp, and geant4
# gdk-pixbuf recipe needs to be patched before the emacs installation
# however in the very beginning the repos/builtin doesn't exist
# can this be avoided by configuring the packages ?
# ------------------------------------------------------------------------------
spack add     frontends@main
spack develop frontends@main
cp $env_name/frontends/scripts/spack.yaml $env_name/.
# if builtin/ doesn't exist, concretize once
#+begin_src
patch -i $env_name/frontends/scripts/gdk-pixbuf-recipe.patch -o spack/var/spack/repos/builtin/packages/gdk-pixbuf/package.py
patch -i $env_name/frontends/scripts/fhicl-cpp-recipe.patch  -o spack-repos/fnal_art/packages/fhicl-cpp/package.py
patch -i $env_name/frontends/scripts/geant4-recipe.patch     -o spack/var/spack/repos/builtin/packages/geant4/package.py
#+end_src
# ------------------------------------------------------------------------------
# concretize and install
# ------------------------------------------------------------------------------
spack concretize -f
spack install

* installing MIDAS on RPI                                                    
- need to disable PostgreSQL
#+begin_src
mkdir daq
cd daq
git clone https://bitbucket.org/tmidas/midas --recurse-submodules
mkdir build
cd build
# if needed, install cmake
sudo apt update
sudo apt install cmake
# after that:
cmake ../midas -DNO_PGSQL=true -DCMAKE_INSTALL_PREFIX=/opt/midas
make
sudo make install
#+end_src

copy additional files (setup script and the tracker frontend itself):

#+begin_src
scp $SPACK_ENV/frontends/scripts/setup_midas.sh       mu2e@rpiXXXX.fnal.gov:~/daq/.
scp $SPACK_ENV/frontends/rpi/rpi_frontend.py          mu2e@rpiXXXX.fnal.gov:~/daq/.

cp $SPACK_ENV/frontends/scripts/start_rpi_frontend.sh $MU2E_DAQ_DIR/config/scripts/.

- to start RPI frontend :

  config/scripts/start_rpi_frontend.sh mu2e-rpiXXX  $ name of the PI

- see the script for additional details
#+end_src

* ROOT interface to mu2e-pcie-utils    validated <2025-04-17 Thu>            
- ROOT interface library is not available by default, you have to build it
- mu2e-pcie-utils depends on artdaq-core-mu2e
#+begin_src                                                                  
export BUILD_ROOT_INTERFACE=1
spack     add mu2e-pcie-utils@develop%gcc@13.1.0

spack concretize -f
spack install mu2e-pcie-utils@develop%gcc@13.1.0
#+end_src
- the installation puts the mu2e-pcie-include files under $SPACK_VIEW/include,
- so the rootlogon.C needs to have the following
#+begin_src                                                                   
{
                                // the line below tells rootcling where to look 
				// for include files

  gInterpreter->AddIncludePath(Form("%s/otsdaq-mu2e-tracker",gSystem->Getenv("SPACK_ENV" )));
  gInterpreter->AddIncludePath(Form("%s/include"            ,gSystem->Getenv("SPACK_VIEW" )));
  gInterpreter->AddIncludePath(Form("%s/include/root"       ,gSystem->Getenv("SPACK_VIEW")));
}
#+end_src
after that, you can do this:
#+begin_src                                                                   
root.exe
root [0] #include "dtcInterface/DTC.hh"
root [1] DTCLib::DTC dtc(DTCLib::DTC_SimMode_NoCFO,-1,0x1,"");
06-18 17:41:07.271940                      DTC_Registers     INFO DTC_Registers(...): Sim Mode is NoCFO
06-18 17:41:07.271997                      DTC_Registers     INFO DTC_Registers(...): DTC ID is 1
06-18 17:41:07.272011                      DTC_Registers     INFO SetSimMode(...): Initializing device, sim mode is NoCFO
06-18 17:41:07.278490                      DTC_Registers     INFO SetSimMode(...): SKIPPING Initializing device
06-18 17:41:07.283713                            DTC.cpp     INFO DTC(...): CONSTRUCTOR
root [1] mu2edev* dev = dtc.GetDevice();
root [2]   uint32_t res; 
root [3]   int      rc;
root [4]   rc = dev->read_register(0x9100,100,&res); printf("0x9100: DTC status       : 0x%08x\n",res); // expect: 0x40808404
0x9100: DTC status       : 0x40808404
#+end_src

*Comments*

- for examples, see https://github.com/Mu2e/otsdaq_mu2e_tracker/blob/develop/scripts/daq.C , 
  or any other .C script in that area
- every script should have the following line on top:
#+begin_src
#define __CLING__ 1
#+end_src
- because of the complexity of the C++ code, ROOT needs to be restarted every time you reload your script
* ------------------------------------------------------------------------------
* back to [[file:frontends.org][frontends]]
* ------------------------------------------------------------------------------
