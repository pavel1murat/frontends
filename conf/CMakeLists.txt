# ----------------------------------------------------------------------
# Build this project's executables:

install(PROGRAMS
        mu2e_config_fe.py
        DESTINATION bin)

# install_fhicl(SUBDIRS fcl)
# install_headers()
# install_source()

